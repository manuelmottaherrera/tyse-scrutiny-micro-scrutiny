# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
#
# IMPORTANT: Shared services (Consul, Kafka, MinIO, MailHog) are now in tyse-infrastructure/
# Start them ONCE from there before running microservices:
#   cd ../tyse-infrastructure && docker compose up -d
#
# This file only contains PostgreSQL specific to this microservice.
name: tysescrutinymicroscrutiny
services:
  postgresql:
    extends:
      file: ./postgresql.yml
      service: postgresql
